-module (heap).

-include("include/values.hrl").

-export ([new/0, new_struct/2, new_array/2, new_lock/2, new_node/3]).

new() ->
	ok.
	
new_struct(Loc, Heap) ->
	ok.
	
new_array(Loc, Heap) ->
	ok.
	
new_lock(Loc, Heap) ->
	ok.
	
new_node(ActivationRef, Node, Heap) ->
	ok.